<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
  </head>
  <body>
    <h1>룸 리스트</h1>

    <table>
      <% room.forEach(item => {
      const { user_id, room_name, room_peo } = item;
      %>
      <form action="" method="POST">
      <tr>
        <th><%=user_id%></th>
        <th><%=room_name%></th>		
        <th><%=room_peo%></th>
        <%url = "/users/room_list/talk?user_id=" + user_id %>	
        <th><a href=<%=url%>>대화 신청하기</a></th>
      </tr>
		  </form>
      <%})%>
    </table>
	<a href="/users/room_insert">방생성</a>
  </body>
</html>
